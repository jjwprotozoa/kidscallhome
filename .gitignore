# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ngrok configuration (contains authtoken)
ngrok.yml

# Environment variables (contains sensitive keys)
.env
.env.local
.env.*.local

# Android build outputs and caches
android/build/
android/app/build/
android/.gradle/
android/.cxx/
android/app/.cxx/

# Android local properties (machine-specific SDK paths)
android/local.properties

# Android keystore files (NEVER commit these!)
android/keystore.properties
android/app/*.jks
android/app/*.keystore
android/**/*.jks
android/**/*.keystore

# Package manager lockfiles (using npm, ignore others)
bun.lockb
yarn.lock
pnpm-lock.yaml
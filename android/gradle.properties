# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Java toolchain version - AGP 8.11.2 requires Java 11+
# Note: In CI/CD, ensure JAVA_HOME points to Java 11+ (Codemagic sets java: 17)
# Explicitly set to Java 21 (LTS) to override any system Java 8 in PATH
org.gradle.java.home=C:\\Program Files\\Java\\jdk-21

android.defaults.buildfeatures.buildconfig=true
android.nonFinalResIds=false

# Ensure Gradle uses Java 11+ for buildscript resolution
# AGP 8.2.1 requires Java 11+ - Gradle will use JAVA_HOME if set
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=true

# If you need to change the Java version, update the org.gradle.java.home path above
# Example for macOS: org.gradle.java.home=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home
# Example for Windows: org.gradle.java.home=C:\\Program Files\\Java\\jdk-21
# Example for Windows (common location): org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.x-hotspot
# 
# IMPORTANT: After changing org.gradle.java.home, you must:
# 1. Stop any running Gradle daemons: gradle --stop (or ./gradlew --stop)
# 2. Restart your IDE to pick up the new Java version
# 3. The Gradle daemon will restart with the new Java version

